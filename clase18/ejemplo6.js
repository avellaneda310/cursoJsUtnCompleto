const vocal = ['a', 'e', 'i'];
const vocalesCompletas = [...vocal, 'o', 'u'];

console.log(vocal);
console.log(vocalesCompletas);

// sireve para traer todas las propiedades de un objeto
// y extrae varias variables a la vez