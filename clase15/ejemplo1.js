const parrafo = document.querySelector('p');// toma solo la primera de las 'P', aunque haya mas 
const parrafos = document.querySelectorAll('p');

console.log(parrafo);
console.log(parrafos);

const destacados = document.querySelectorAll('.destacados');// el punto es la clase y va si o si

console.log(destacados)